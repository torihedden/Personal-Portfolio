<img src="" alt="Map of appointment location" height="" width="">

//Google Static Maps API image is embedded within an <img> tag's src attribute
//https://maps.googleapis.com/maps/api/staticmap?parameters
//center (required if markers not present) defines the center of the map, equidistant from all edges of the map
//or a string address (e.g. "city hall, new york, ny") identifying a unique location on the face of the earth
//size = a string of the form {horizontal_value}x{vertical_value}
//scale defaults to 1
//maptype (optional) defines the type of map to construct. There are several possible maptype values, including roadmap
//string should be URL-escaped, so addresses such as "City Hall, New York, NY" should be converted to "City+Hall,New+York,NY"
//
